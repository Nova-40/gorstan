import '@testing-library/jest-dom';
// Flag to allow components (e.g., SplashScreen) to shorten delays under test
process.env.TEST_MODE = 'true';
// Global test setup can go here (mock audio, timers, etc.)
